```mermaid
    sequenceDiagram
        participant browser
        participant server

        browser->server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa as JSON data
        activate server
        server-->browser: {"message":"note created"}
        deactivate server

        Note to browser , execute the js code in spa.js to fetch and render the data.json and then post.